import styled from 'styled-components/native';
import colourVars from '../../../assets/styles/colours';

const { SIGray2 } = colourVars;

const NoContent = styled.Text`
    text-align: center;
    margin: 40px 0;
    color: ${SIGray2};
`;

export default NoContent;
