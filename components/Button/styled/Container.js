import styled from 'styled-components/native';

export default (Container = styled.TouchableOpacity`
    padding: 10px;
    background-color: #ffcc00;
    border: 1px solid rgba(0, 0, 0, 0.07);
    border-radius: 3px;
`);
